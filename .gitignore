node_modules/
coverage/
.env
.env.*
!.env.example
.DS_Store
.cursor/
*.log
*.tmp

# Runtime state for the local JSON driver. All generated: the catalogue is hydrated from
# data/puzzles.seed.json (the tracked source of truth) on first boot, and mutated by the admin
# UI thereafter - so committing it just guarantees drift.
data/puzzles.json
data/players.json
data/attempts.json
